<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
    <style name="any" parent="android:style/Animation.Activity"/>
    <style name="BottomNavigation">
        <item name="android:textSize">14sp</item> <!-- Change the text size if needed -->
        <item name="android:textColor">@color/white</item> <!-- Change the text color if needed -->
        <item name="android:drawablePadding">0dp</item> <!-- Adjust padding if needed -->
        <item name="android:fontFamily">@font/imbue</item> <!-- Change the font family if needed -->
        <item name="android:width">24dp</item> <!-- Change the width of the icon -->
        <item name="android:height">24dp</item> <!-- Change the height of the icon -->
    </style>
</resources>